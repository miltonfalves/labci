name: Docker

on:
    workflow_call:

jobs:

  docker:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Setup Docker Build
      uses: docker/setup-buildx-action@v2.1.0
      
    - name: teste do secret
      run: |
        echo "$TESTE"
      env:
        TESTE: ${{ secrets.TESTE }}
